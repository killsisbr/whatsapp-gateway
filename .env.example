# Porta do servidor
PORT=3000

# Nome do browser exibido no WhatsApp
BROWSER_NAME=WhatsApp Gateway

# Webhook padrão (opcional) — todos os projetos registram via API
DEFAULT_WEBHOOK_URL=

# Intervalo de reconexão em ms
RECONNECT_INTERVAL=5000
